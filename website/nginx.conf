server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    error_page 404 /404.html;

    # Serve static files directly, return 404 for missing files
    location / {
        try_files $uri $uri/ =404;
    }
}
