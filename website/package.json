{
  "name": "binschema-website",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "prepare:docs": "node scripts/prepare-docs.js",
    "dev": "npm run prepare:docs && vite",
    "build": "npm run prepare:docs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.8"
  },
  "dependencies": {
    "binschema": "workspace:*",
    "prismjs": "^1.30.0",
    "codemirror": "^6.0.0",
    "@codemirror/lang-json": "^6.0.0",
    "@codemirror/state": "^6.0.0",
    "@codemirror/theme-one-dark": "^6.0.0"
  }
}
