{
  "name": "binschema-workspace",
  "version": "0.4.0",
  "private": true,
  "description": "BinSchema monorepo workspace",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "just-release": "^0.8.2"
  }
}
